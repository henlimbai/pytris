just a python-tetris implementation using the pygame library.

To install the pygame library in ubuntu, just run:
sudo apt-get install python3-pygame

in others SOs:
https://www.pygame.org/wiki/GettingStarted
